import styled from "styled-components";

export const Success = styled.div`
    color: var(--white);

    h3 {
        font-size: 16px;
        font-weight: 700;
        margin-bottom: 16px;
    }

    p {
        font-size: 14px;
        font-weight: 400;
    }    
`
export const Overlay = styled.div`
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
`